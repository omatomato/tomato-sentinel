[project]
name = "tomato-sentinel-workspace"
version = "0.0.0"
description = "Foundation workspace for Tomato Sentinel"
requires-python = ">=3.13,<3.15"
dependencies = []

[dependency-groups]
dev = [
  "jsonschema==4.26.0",
  "mypy==2.3.0",
  "pytest==9.1.1",
  "ruff==0.15.22",
]

[tool.uv]
package = false
required-version = "==0.11.29"

[tool.pytest.ini_options]
addopts = ["-ra", "--strict-config", "--strict-markers"]
pythonpath = ["packages/policy-engine/src"]
testpaths = ["packages", "tests"]

[tool.mypy]
python_version = "3.13"
strict = true
files = ["packages/policy-engine/src", "packages/policy-engine/tests"]
warn_unreachable = true

[tool.ruff]
target-version = "py313"
line-length = 88
extend-exclude = ["docs/product/product-specification-original.md"]

[tool.ruff.lint]
select = ["B", "C4", "E", "F", "I", "RUF", "SIM", "UP"]

[tool.ruff.lint.per-file-ignores]
"**/tests/**/*.py" = ["S101"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
